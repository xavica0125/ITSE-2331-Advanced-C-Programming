#pragma once

#include <iostream>
#include <string>

#include <set>
#include <map>

using std::cout; using std::cin; using std::string;

using std::set; using std::multiset; using std::map; using std::multimap;

void orderedAssociativeChoice(const int choice);

void useSet();

void useMultiset();

void useMap();

void useMultimap();