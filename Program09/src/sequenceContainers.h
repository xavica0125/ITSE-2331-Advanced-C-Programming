#pragma once

#include <iostream>
#include <string>

#include <algorithm>
#include <array>
#include <deque>
#include <forward_list>
#include <list>
#include <vector>

using std::cout; using std::cin; using std::string;

using std::array; using std::deque; using std::forward_list; using std::list; using std::vector; using std::sort;

void sequenceContainerChoice(const int choice);

void useArray();

void useDeque();

void useForwardList();

void useList();

void useVector();