#pragma once

#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <array>
#include <set>

using std::cout; using std::cin; using std::string; using std::vector; using std::array; using std::set;

using std::binary_search; using std::upper_bound; using std::set_difference; using std::sort; using std::set_union; using std::equal_range;

void sortingAlgoChoice(const int choice);

void binarySearchAlgo();

void upperBoundAlgo();

void setDifferenceAlgo();

void sortAlgo();

void setUnionAlgo();

void equalRangeAlgo();