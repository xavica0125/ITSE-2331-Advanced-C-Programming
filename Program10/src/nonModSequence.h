#pragma once

#include <iostream>
#include <string>
#include <algorithm>
#include <vector>

using std::cout; using std::cin; using std::string; using std::vector; 

using std::find; using std::count; using std::equal; using std::for_each;

void nonModAlgoChoice(const int choice);

void findAlgo();

void countAlgo();

void equalAlgo();

void forEachAlgo();