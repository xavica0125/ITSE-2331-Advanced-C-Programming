#pragma once

#include <iostream>
#include <string>
#include <algorithm>
#include <numeric>
#include <vector>
#include <map>

using std::cout; using std::cin; using std::string; using std::vector; using std::map;

using std::accumulate; using std::transform;

void numericalAlgoChoice(const int choice);

void accumulateAlgo();

void transformAlgo();
