#pragma once

#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <array>

using std::cout; using std::cin; using std::string; using std::vector; using std::array;

using std::generate; using std::fill_n; using std::replace_if; using std::swap_ranges;

void modAlgoChoice(const int choice);

int nextNum();

void generateAlgo();

void fill_nAlgo();

void replace_ifAlgo();

void swap_rangesAlgo();


