#pragma once
#include <string>
#include <iostream>
#include <array>

using std::string; using std::cout; using std::cin; using std::getline; using std::array;

bool characterAndDragonEntry();
void arrayLoop();
void arrayOutput(int& characterIndex, int (&numDragons)[50], string (&characters)[50][50]);